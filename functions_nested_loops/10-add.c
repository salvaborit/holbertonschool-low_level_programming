#include "main.h"
/**
* add - adds two integers and returns the result
* @a: var that stores first int to be added
* @b: var that stores second int to be added
* Return: result of addition
*/
int add(int a, int b)
{
	int res;

	res = a + b;
	return(res);
}
