#include "main.h"
#include <stdio.h>
/**
* mul - multiplies two integers
* @a: stores first number to be multiplied
* @b: stores second number to be multiplied
* Return: Always 0 (Success)
*/
int mul(int a, int b)
{
	int res;

	res = a * b;
	printf("%d", res);
}
